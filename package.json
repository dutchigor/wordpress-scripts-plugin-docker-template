{
  "name": "your-plugin",
  "version": "1.0.0",
  "description": "",
  "main": "assets/build/index.js",
  "scripts": {
    "start": "npm-run-all --parallel dockerup watch sync",
    "sync": "node browser-sync.js",
    "build": "wp-scripts build",
    "watch": "wp-scripts start",
    "dockerup": "docker compose --env-file docker.env --file ./docker/docker-compose.yml up",
    "dockerdown": "docker compose --env-file docker.env --file ./docker/docker-compose.yml down",
    "wp": "docker compose --env-file docker.env --file ./docker/docker-compose.yml run wordpress wp"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@wordpress/scripts": "^24.6.0",
    "browser-sync": "^2.27.10",
    "dotenv": "^16.0.3",
    "npm-run-all": "^4.1.5",
    "wait-on": "^6.0.1"
  }
}
